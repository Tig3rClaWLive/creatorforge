'use client';
import { useState } from 'react';
export default function Register(){const [msg,setMsg]=useState(''); async function submit(e:any){e.preventDefault(); const data=Object.fromEntries(new FormData(e.currentTarget)); const res=await fetch('/api/register',{method:'POST',body:JSON.stringify(data)}); const j=await res.json(); setMsg(j.message||j.error); if(res.ok) location.href='/dashboard';} return <section className="container py-16 max-w-2xl"><h1 className="text-5xl font-black">Registrieren</h1><form onSubmit={submit} className="card mt-8 space-y-5 p-6"><label className="label">E-Mail<input className="input mt-2" name="email" type="email" required/></label><label className="label">Passwort<input className="input mt-2" name="password" type="password" minLength={8} required/></label><label className="label">Creator-Name<input className="input mt-2" name="display_name" required/></label><button className="btn btn-primary w-full">Account erstellen</button>{msg&&<p className="text-sm text-orange-300">{msg}</p>}</form></section>}
