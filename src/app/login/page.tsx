'use client';
import { useState } from 'react';
export default function Login(){const [msg,setMsg]=useState(''); async function submit(e:any){e.preventDefault(); const data=Object.fromEntries(new FormData(e.currentTarget)); const res=await fetch('/api/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(data)}); const j=await res.json(); setMsg(j.message||j.error); if(res.ok) location.href='/dashboard';} return <section className="container max-w-2xl py-16"><h1 className="text-5xl font-black">Login</h1><form onSubmit={submit} className="card mt-8 space-y-5 p-6"><label className="label">E-Mail<input className="input mt-2" name="email" type="email" required/></label><label className="label">Passwort<input className="input mt-2" name="password" type="password" required/></label><button className="btn btn-primary w-full">Einloggen</button>{msg&&<p className="text-sm text-orange-300">{msg}</p>}</form></section>}
